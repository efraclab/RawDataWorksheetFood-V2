import React from "react";
import EmptySamplePreparation from "./EmptySamplePreparation";
export interface SamplePreparationSectionProps<T>{title:string; preparations:readonly T[]; isLocked:boolean; onAddPreparation:()=>void; onRemovePreparation?:(preparation:T,index:number)=>void; renderPreparation:(preparation:T,index:number)=>React.ReactNode;}
function SamplePreparationSection<T>({title,preparations,isLocked,onAddPreparation,onRemovePreparation,renderPreparation}:SamplePreparationSectionProps<T>){return <section className="p-6"><div className="flex items-center justify-between mb-6"><h3 className="text-lg font-bold text-emerald-800 flex items-center gap-2.5 tracking-tight"><span className="w-1.5 h-6 bg-gradient-to-b from-emerald-700 to-emerald-900 rounded-full"/>{title}</h3><button type="button" onClick={onAddPreparation} disabled={isLocked} className="rounded-xl bg-emerald-600 hover:bg-emerald-700 px-5 py-2.5 text-sm font-semibold text-white shadow-lg disabled:opacity-50 disabled:cursor-not-allowed">+ Add Preparation</button></div>{preparations.length===0?<EmptySamplePreparation onAddPreparation={onAddPreparation} isLocked={isLocked}/>:<div className="space-y-5">{preparations.map((p,i)=><React.Fragment key={i}>{renderPreparation(p,i)}</React.Fragment>)}</div>}</section>}
export default SamplePreparationSection;
