import React from "react";
import { motion } from "framer-motion";
import { Target } from "lucide-react";
export interface EmptySamplePreparationProps { title?:string; description?:string; isLocked?:boolean; onAddPreparation:()=>void; }
const EmptySamplePreparation:React.FC<EmptySamplePreparationProps>=({title="No sample preparations added yet",description="Click the add button to create LOD sample preparation",isLocked=false,onAddPreparation})=><motion.div initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} className="relative overflow-hidden text-center py-12 bg-gradient-to-br from-emerald-50 via-white to-emerald-50 border-2 border-dashed border-emerald-300 rounded-2xl shadow-inner"><div className="relative z-10"><div className="inline-block p-4 bg-white rounded-full shadow-lg mb-3"><Target className="w-10 h-10 text-emerald-400"/></div><p className="text-base font-bold text-emerald-800 mb-1">{title}</p><p className="text-xs text-emerald-600/80 max-w-md mx-auto">{description}</p><button type="button" onClick={onAddPreparation} disabled={isLocked} className="mt-6 inline-flex items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-100/50 px-4 py-2 text-xs font-semibold text-emerald-800 disabled:opacity-50"><span className="h-2 w-2 rounded-full bg-emerald-500"/>Ready to start</button></div></motion.div>;
export default EmptySamplePreparation;
