const n=(v:unknown):number|null=>{if(v===null||v===undefined||String(v).trim()==="")return null;const x=Number(v);return Number.isFinite(x)?x:null;};
export interface CalciumCalculationInput{sample:unknown;df:unknown;edta:unknown;strength:unknown;}
export interface CalciumCalculationResult{success:boolean;result:number|null;resultSecondary:number|null;error?:string;}
export function calculateCalcium(i:CalciumCalculationInput):CalciumCalculationResult{const sample=n(i.sample),df=n(i.df),edta=n(i.edta),strength=n(i.strength);if([sample,df,edta,strength].some(v=>v===null))return{success:false,result:null,resultSecondary:null,error:"All Calcium values are required."};if(sample!<=0||df!<=0||edta!<0||strength!<0)return{success:false,result:null,resultSecondary:null,error:"Sample volume and dilution factor must be greater than zero; EDTA values cannot be negative."};const hardness=(edta!*strength!*100*1000*df!)/sample!;return{success:true,result:Math.trunc(hardness*1000)/1000,resultSecondary:Math.trunc(hardness*0.4008*1000)/1000};}
