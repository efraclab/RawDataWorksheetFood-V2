import { useMemo, useState } from "react";
import { ChevronDown, ChevronUp, Trash2 } from "lucide-react";
import CustomDropdown from "../../../../../shared/CustomDropdown";
import type { CalculationTotalHardness } from "../models/CalculationTotalHardness";
import type { SamplePreparationTotalHardness } from "../models/SamplePreparationTotalHardness";
import { calculateTotalHardness } from "../calculation";
const text = (value: unknown) => (value == null ? "" : String(value));
const numberOrNull = (value: unknown) => { const parsed = Number(text(value).trim()); return text(value).trim() === "" || !Number.isFinite(parsed) ? null : parsed; };
const step = (preparation: SamplePreparationTotalHardness | null, name: string) => preparation?.steps.find((item) => item.name.trim().toLowerCase() === name.trim().toLowerCase());
interface Props { calculation: CalculationTotalHardness; samplePreparations: SamplePreparationTotalHardness[]; onUpdate: (calculation: CalculationTotalHardness) => void; onRemove: () => void; isLocked: boolean; }
export default function CalculationDetailTotalHardness({ calculation, samplePreparations, onUpdate, onRemove, isLocked }: Props) {
  const [expanded, setExpanded] = useState(true);
  const selected = useMemo(() => samplePreparations.find((item) => item.label === calculation.selectedSamplePreparationLabel) ?? samplePreparations[0] ?? null, [samplePreparations, calculation.selectedSamplePreparationLabel]);
  const values = useMemo(() => {
    const sampleVolume = step(selected, "Volume of sample");
    const df = step(selected, "Dilution factor");
    const edtaVolume = step(selected, "Volume of EDTA");
    const strength = step(selected, "Strength of EDTA");
    return {
      sampleVolume: sampleVolume?.value1 ?? "",
      df: df?.value1 ?? "",
      edtaVolume: edtaVolume?.value1 ?? "",
      strength: strength?.value1 ?? ""
    };
  }, [selected]);
  const requiredKeys = ["sampleVolume", "df", "edtaVolume", "strength"] as const;
  const errors = requiredKeys.filter((key) => text(values[key]).trim() === "" || numberOrNull(values[key]) === null).map((key) => `${key} is required and must be numeric`);
  const update = <K extends keyof CalculationTotalHardness>(field: K, value: CalculationTotalHardness[K]) => onUpdate({ ...calculation, [field]: value });
  const runCalculation = () => { if (isLocked || !selected || errors.length > 0) return; const result = calculateTotalHardness({ sampleVolume: values.sampleVolume, df: values.df, edtaVolume: values.edtaVolume, strength: values.strength }); onUpdate({ ...calculation, selectedSamplePreparationLabel: selected.label, calculationResult: result.success ? result.result : null, calculationResultUnit: "mg/L" }); };
  const resultNumber = numberOrNull(calculation.calculationResult); const minimum = numberOrNull(calculation.acceptanceLimitMin); const maximum = numberOrNull(calculation.acceptanceLimitMax); const hasLimits = minimum !== null || maximum !== null; const passed = resultNumber !== null && (minimum === null || resultNumber >= minimum) && (maximum === null || resultNumber <= maximum);
  return <div className="overflow-hidden rounded-xl border-2 border-emerald-200 bg-white shadow-lg"><div className="flex items-center justify-between bg-gradient-to-r from-emerald-700 via-emerald-800 to-slate-900 px-4 py-3 text-white"><button type="button" onClick={() => setExpanded((v) => !v)} className="font-semibold">{calculation.label} · Total Hardness (as CaCO3)</button><div className="flex gap-2"><button type="button" onClick={() => setExpanded((v) => !v)}>{expanded ? <ChevronUp /> : <ChevronDown />}</button><button type="button" disabled={isLocked} onClick={onRemove}><Trash2 /></button></div></div>{expanded && <div className="space-y-5 p-6"><div className="rounded-lg border border-emerald-200 p-4"><label className="mb-2 block text-sm font-bold">Select Sample Preparation</label><CustomDropdown options={samplePreparations.map((item) => ({ value: item.label, label: item.label }))} value={calculation.selectedSamplePreparationLabel ?? ""} onChange={(value) => onUpdate({ ...calculation, selectedSamplePreparationLabel: value || null, calculationResult: null })} colorScheme="emerald" disabled={isLocked} /></div>{selected && <><div className="rounded-lg border border-emerald-200 bg-slate-50 p-4 text-center font-mono text-xs">Total Hardness (CaCO3), mg/L = (Volume of EDTA × Strength of EDTA × 100 × 1000 × Dilution factor) / Volume of sample</div>{errors.length > 0 && <div className="rounded-lg border-2 border-red-200 bg-red-50 px-5 py-4"><h4 className="mb-2 text-sm font-bold text-red-800">Validation Errors</h4><ul className="space-y-1">{errors.map((error) => <li key={error} className="text-xs text-red-700">• {error}</li>)}</ul></div>}<div className="rounded-lg border border-emerald-200 p-4"><h4 className="mb-3 font-bold">Acceptance Limit</h4><div className="grid grid-cols-1 gap-3 md:grid-cols-2"><input disabled={isLocked} value={text(calculation.acceptanceLimitMin)} onChange={(e) => update("acceptanceLimitMin", e.target.value)} placeholder="Minimum Limit" className="w-full rounded-lg border border-emerald-300 px-3 py-2 outline-none hover:border-emerald-500 focus:border-emerald-500" /><input disabled={isLocked} value={text(calculation.acceptanceLimitMax)} onChange={(e) => update("acceptanceLimitMax", e.target.value)} placeholder="Maximum Limit" className="w-full rounded-lg border border-emerald-300 px-3 py-2 outline-none hover:border-emerald-500 focus:border-emerald-500" /></div></div><div className="text-center"><button type="button" disabled={!selected || errors.length > 0} onClick={runCalculation} className="rounded-lg bg-gradient-to-r from-emerald-700 to-slate-900 px-6 py-3 font-semibold text-white disabled:cursor-not-allowed disabled:opacity-50">Calculate Result</button></div>{calculation.calculationResult !== null && <div className="overflow-hidden rounded-lg border-2 border-emerald-300"><div className="bg-gradient-to-r from-emerald-700 via-emerald-800 to-slate-900 px-4 py-2 font-bold text-white">Total Hardness (as CaCO3) Result</div><div className="flex items-center gap-3 p-4 text-2xl font-bold"><span>{text(calculation.calculationResult)} {calculation.calculationResultUnit || "mg/L"}</span>{hasLimits && <span className={`rounded-full border px-3 py-1 text-sm ${passed ? "border-green-300 bg-green-100 text-green-800" : "border-red-300 bg-red-100 text-red-800"}`}>{passed ? "Pass" : "Fail"}</span>}</div></div>}</>}</div>}</div>;
}
