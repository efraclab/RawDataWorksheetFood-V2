import type { LabPlugin } from "../../core/plugin";

const environmentPlugin: LabPlugin = {
  id: "environment",

  name: "Environment",

  description:
    "Environment laboratory plugin.",

  preparations: [],
};

export default environmentPlugin;